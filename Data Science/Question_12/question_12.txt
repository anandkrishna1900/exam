# 12. Import and Export Data into and from Text File in R

students <- data.frame(
  ID = c(1, 2, 3),
  Name = c("John", "Alice", "Bob"),
  Marks = c(85, 90, 78)
)

write.table(students, "students.txt")

students <- read.table("students.txt", header = TRUE)
print(students)
